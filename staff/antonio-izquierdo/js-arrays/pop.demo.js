console.log('DEMO pop')

var colors = ['black', 'red', 'blue', 'yellow'];

console.log('Remove the last element of the array "colors" to the log', colors);

pop(colors)
console.log(colors);

var cities = ['Barcelona', 'Madrid', 'Valencia'];

console.log('Remove the last element of the array "cities" to the log', cities);

pop(cities)
console.log(cities);