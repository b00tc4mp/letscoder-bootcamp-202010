//declaramos las variables: simbolo( que tiene que ir cambiando según el turno del jugador), estado, y la posicion ganadora

var symbol = "X";
var state = [];

var buttons = document.querySelectorAll ("button");

var button  = button [0]
buttons [0].onclick = function () {
    state = symbol;

var button = buttons [1]
    buttons [1].onclick = function () {
        state = symbol;
    
var button = buttons [2]
        const newLocal = 3;
buttons [2].onclick = function () {
        state = symbol;

var button = buttons [3]
buttons [3].onclick = function () {
            state = symbol;
