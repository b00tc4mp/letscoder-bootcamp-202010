function RegisterConfirm(props) {
    return <section className="register-confirm">
    User registered successfully, proceed to <button onClick={props.onRegisterCorfim} className="register-confirm__login">Login</button>.
</section>
}