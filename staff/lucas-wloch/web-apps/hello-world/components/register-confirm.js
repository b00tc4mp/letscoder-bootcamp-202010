function mountRegisterConfirm(selector,onClick) {
    var confirm = document.querySelector('selector');
    var button = document.querySelector('.confirm__button');

    button.onclick = onClick;
}  
    